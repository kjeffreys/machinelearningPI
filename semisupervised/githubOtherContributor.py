'''
Oh noes! Another user has started contributing (not really).

Now we need to make sure to use proper git in order to merge changes rather than overwrite.

Creating this file as a file that was not locally on my machine. Then creating local change on machine.

Testing that there is successful update when using git add/commit, then git pull, then git push after resolving any conflicts
'''

if __name__ == "__main__":
    print('success!')